apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: cooknect
  labels:
    app: gateway-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      serviceAccountName: default
      automountServiceAccountToken: false
      containers:
        - name: gateway-service
          image: cooknect/gateway-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka-service:9092"
            - name: SERVER_PORT
              value: "8080"
            - name: USER_SERVICE_URL
              value: "http://user-service:8080"
            - name: RECIPE_SERVICE_URL
              value: "http://recipe-service:8080"
            - name: CHALLENGE_SERVICE_URL
              value: "http://challenge-service:8080"
            - name: NUTRITION_SERVICE_URL
              value: "http://nutrition-service:8080"
            - name: MEALPLANNER_SERVICE_URL
              value: "http://mealplanner-service:8080"
            - name: NOTIFICATION_SERVICE_URL
              value: "http://notification-service:8080"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
              ephemeral-storage: "100Mi"
            limits:
              memory: "1Gi"
              cpu: "500m"
              ephemeral-storage: "500Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: cooknect
  labels:
    app: gateway-service
spec:
  selector:
    app: gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: LoadBalancer
