apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-recipe-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-recipe-service
  template:
    metadata:
      labels:
        app: postgres-recipe-service
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "recipe_service_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "admin"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-recipe-service-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-recipe-service-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-recipe-service
spec:
  selector:
    app: postgres-recipe-service
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
