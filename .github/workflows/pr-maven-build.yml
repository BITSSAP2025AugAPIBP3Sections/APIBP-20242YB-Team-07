name: PR Maven Build

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  discover:
    name: Discover Microservices
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.set-matrix.outputs.services }}

    steps:
      - uses: actions/checkout@v4

      - id: set-matrix
        run: |
          echo "🔍 Finding all backend microservices..."
          SERVICES=$(find backend -mindepth 1 -maxdepth 1 -type d -exec test -f "{}/pom.xml" \; -print | xargs -n1 basename | jq -R -s -c 'split("\n")[:-1]')
          echo "services=$SERVICES" >> $GITHUB_OUTPUT
          echo "✅ Found services: $SERVICES"

  build:
    name: Build Services
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: ${{ fromJson(needs.discover.outputs.services) }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build ${{ matrix.service }}
        run: mvn -f backend/${{ matrix.service }}/pom.xml clean package -DskipTests
